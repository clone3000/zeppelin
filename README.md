Задание выполнил через докер-контейнер. Аккаунт в яндекс клауде был изначально заблокирован по неизвестным причинам. В него же был добавлен промокод + стартовый грант. Переговоры результатов не дали. Создавал второй аккаунт. При создании кластера долгая загрузка около 30 минут, результат : 'dead'. Так же пробовал несколько раз пересоздавать его, полностью повторяя процедуру создания как из лекции. Все тот же 'dead'. Решил попробовать альтернативный вариант. Сохранинени в облачное хранилище в задании не использовалось. Только HDFS.


Сборка витрины mart_city_top_products

Инструменты:

Apache Zeppelin (%spark.pyspark)
PySpark (DataFrame API, оконные функции)
HDFS (сохранение в Parquet)

Выполненые пункты:

Расчет revenue = qty * price
Объединение join orders + users + products
Метрики  orders_cnt, qty_sum, revenue_sum по city, product_id, product_name
Оконная функция top-2 по revenue_sum для каждого города
Сохранение в HDFS (/tmp/sandbox_zeppelin/mart_city_top_products/, Parquet, overwrite)
Чтение из Parquet и вывод результата
